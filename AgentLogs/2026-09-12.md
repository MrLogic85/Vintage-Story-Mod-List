# 2026-09-12

## Investigated: SmithingPlus vs. Toolsmith "several tool heads to repair" report

User reported (in Swedish): when one metal tool breaks, several tool heads end up needing repair — suspected conflict between SmithingPlus and Toolsmith.

No relevant errors/exceptions found in `Logs/client-main.log`, `Logs/client-debug.log`, or the server logs — both mods load cleanly and no crash/warning mentions either mod near tool breakage. This is a config/design overlap, not a crash bug.

Root cause hypothesis, from inspecting `ModConfig/SmithingPlus.json` and the `toolsmith_1.2.19.zip` assets:

- SmithingPlus has its own broken-tool-head recovery feature (`EnableToolRecovery: true`, `DontRepairBrokenToolHeads: false`) that applies to any item whose *code* matches `ToolHeadSelector`: `@(.*)(head|blade|boss|barrel|stirrup|part)(.*)`.
- Toolsmith represents *every* tool head — any tool type, any material, metal or nonmetal — as a single shared item type/code, `toolsmith:tinkertoolparts` (`assets/toolsmith/itemtypes/tinkertoolparts.json`). The specific tool/material is stored in item attributes (via its `ModularPartRenderingFromAttributes` behavior), not encoded in the item code.
- Because `tinkertoolparts` contains the substring "part", it matches SmithingPlus's selector. So SmithingPlus's own anvil-based head-recovery/repair bookkeeping — which appears to key off item code — gets applied to Toolsmith's heads too, on top of Toolsmith's own dedicated head/handle/binding repair-at-Workbench system. Since all Toolsmith heads share that one code, SmithingPlus's recovery bookkeeping can't distinguish "this specific broken head" from "every tinkertoolparts item that happens to be damaged," which plausibly explains why breaking one tool surfaces several heads as repairable at once.
- Toolsmith's own MOD_DB.json entry documents only one SmithingPlus integration: disabling its internal "Smithing with Bits" system so the two don't duplicate bit-smithing. This `ToolHeadSelector` overlap isn't handled by either mod.

**Confirmed** via the mods' own pages (checked after the user asked whether the pages said anything):

- Toolsmith's page: explicitly "designed from the ground up to work alongside Smithing Plus" — its Workbench reforges broken tool heads using SmithingPlus's anvil recipes. Recommends lowering SmithingPlus's `BrokenToolVoxelPercent` to 0.4 (installed config currently has 0.8) so head-breakage is penalized accurately.
- SmithingPlus's page: has user comments reporting the *exact* symptom — "Broke a copper axe head, ended up with six broken copper axe heads in inventory" — with a moderator reply: "You have to disable Tool Recovery."

So this is a known community-reported bug, not just a local hypothesis. The specific culprit is SmithingPlus's `EnableToolRecovery` feature (distinct from the Workbench-reforging-via-anvil-recipes path Toolsmith relies on, which is unaffected by disabling it).

Remedy applied 2026-09-12 at user's request, editing `ModConfig/SmithingPlus.json`:
- `EnableToolRecovery`: `true` → `false` — the moderator-recommended fix for the duplication bug.
- `BrokenToolVoxelPercent`: `0.8` → `0.4` — Toolsmith's own recommended value for accurate head-breakage penalties (unrelated to the duplication bug, but a documented compat tweak).

Documented in MOD_DB.json: added to `open_flags`, and added/updated a `checked` entry (dated 2026-09-12) to both `smithingplus` and `toolsmith` mod entries citing the confirmed mod-page sources and noting the fix was applied. This is a personal ModConfig/ file (gitignored, not part of the tracked repo) — nothing to commit/push for this change.

## Updated docs/smithing-and-crafting.md and pushed

Added a "Known issue with default settings" note under the Toolsmith section explaining the duplication bug and the fix, since the page's existing "Runs alongside SmithingPlus cleanly" line was now misleading on its own.

Pushed together with two other pending sessions' curation-file changes (2026-09-08 crash-theory refinement, 2026-09-10 HudClock/xSkills fix) that hadn't been committed yet — commit `cc69064` (beafb06..cc69064), game version v1.22.7, 52 mods installed (unchanged this session; see commit message for the full list).

## New AGENTS.md rule: document cross-mod fixes in the user guide, not just MOD_DB.json

User asked to add a rule: whenever a change is made to get two mods working together, it must also be documented in the relevant docs/*.md page, not just recorded in MOD_DB.json. Added as a new bullet after the existing "Cross-mod settings" rule. Also fixed an unrelated gap noticed while editing: the tracked-files list in the push-procedure bullet was missing `docs/` even though it's already tracked and pushed (confirmed via `git ls-files docs/`).

This immediately surfaced a real gap: the 2026-09-10 HudClock/xSkills HUD-overlap fix was documented in MOD_DB.json (and already pushed) but never made it into `docs/quality-of-life.md` or `docs/progression.md` — user asked about it, confirmed via `grep` across all docs/*.md that it wasn't mentioned anywhere.

Added a note to both pages (HUD Clock section of quality-of-life.md, and the xSkills compatibility list in progression.md): the fix lives in `ModConfig/XLeveling/gui/xSkillsGilded.json`, setting `effectBoxOriginX`/`effectBoxOriginY` (default `8`/`8`) to `13`/`122`. Flagged explicitly per the user's own uncertainty: these are raw pixel coordinates, not scaled to resolution or UI scale — confirmed working at the user's 2560×1440, but likely needs retuning at other resolutions/UI scales (not verified either way, no way to test that here).

## Follow-up: Toolsmith tool broke with no head at all

User broke a Tinkered Tool (handle+cord, assembled by holding right-click) after this session's `EnableToolRecovery: false` fix, and got zero heads back rather than one — asked whether it was the recovery-toggle fix or the head being too damaged.

Checked decompiled strings in `Mods/toolsmith_1.2.19.zip`'s `Toolsmith.dll`: its own break handler (`HandleBrokenTinkeredTool`) has no string references to `EnableToolRecovery`, `ToolHeadSelector`, or `DontRepairBrokenToolHeads` — so today's earlier fix isn't implicated. (Only checked for those string references in Toolsmith's own break-handling method; did not decompile the reforge-threshold math itself, so this rules out one cause without fully confirming the other.)

User's own hypothesis, which matches the config: they'd been sharpening the head continuously during use. `ModConfig/Toolsmith.json` has `ShouldHoningDamageHead: true` (default) and `HoningDamageMult: 1.0` — sharpening at a Grindstone/Whetstone adds wear to the head on top of normal use-wear. Combined with `PercentDamageForReforge: 1.0`, sustained sharpening plausibly pushed the head's cumulative damage past whatever ceiling keeps a broken head reforge-eligible, so it was destroyed outright instead of dropping as a repairable head.

Remedy applied at user's request, editing `ModConfig/Toolsmith.json`:
- `ShouldHoningDamageHead`: `true` → `false` — sharpening no longer contributes to head wear/destruction.

Documented in MOD_DB.json as a new `checked` entry on `toolsmith` (dated 2026-09-12). Not added to `docs/smithing-and-crafting.md` — this is a single-mod tuning choice, not a cross-mod compatibility fix (per the AGENTS.md rule added earlier this session, which only requires doc updates for the latter).
