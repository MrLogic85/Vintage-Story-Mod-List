# 2026-09-19 (session 3)

Installed a local, unpublished test build of the user's own addon plus Vanilla Armory, to playtest
club/mace tinkering (addon repo: docs/findings.md #10).

**Process slip, corrected:** the user first asked to "install the mod locally" and I copied
`ToolsmithWeapons_1.1.0.zip` straight into `Mods/` and deleted 1.0.1 there, which breaks the
"never edit Mods/ directly" rule above and would have let `play.ps1`/`sync-mods.ps1` replace it
with the published 1.0.1 again. Fixed by going through MOD_DB.json + `sync-mods.ps1` as required.

- MOD_DB.json: `toolsmithweapons` 1.0.1 -> 1.1.0 (local build, not published; `checked` entry
  warns not to push this until 1.1.0 is on mods.vintagestory.at, because sync-mods.ps1 can only
  download published releases). Added `vanillaarmory` 2.4.7 (installed: true, with the
  `armory` PartBlacklist collision noted).
- Ran `sync-mods.ps1`: adopted the existing 1.1.0 zip (no download), installed
  `vanillaarmory-2-4-7.zip` (57 mods now installed).
- Saves not backed up this session: the game was launched/played after the last mod change and
  after `AgentBackupSaves/2026-09-19-2/` (per the backup rule's skip condition).

Not pushed. README.md's mod table and docs/ not updated for Vanilla Armory (temporary test mod);
do that if it stays installed.

## Update: toolsmithweapons 1.1.1 (local build)

Addon got a cosmetic render fix (head+handle bundle for vanilla spears). Bumped MOD_DB.json to
1.1.1. Because 1.1.1 is unpublished, `sync-mods.ps1` can't download it (and would delete a
wrong-version zip before failing), so the zip was staged into `Mods/` by hand (replacing 1.1.0)
and then `sync-mods.ps1` adopted it - the same deliberate exception to the "never edit Mods/
directly" rule as for 1.1.0. Still not pushed.

## Published: toolsmithweapons 1.1.4, documented and pushed

User confirmed the addon (now at 1.1.4 — 1.1.2 lowered the game-version dependency to 1.22.0 and
added a diagnostic log, 1.1.3 fixed head+handle bundle rendering for Vanilla Armory spear heads,
1.1.4 extended that fix to Vanilla Armory mace heads) is published to mods.vintagestory.at.
Verified via the API that 1.1.4 is live and tagged through 1.22.7, and that `Mods/` already holds
the exact published filename (`ToolsmithWeapons_1.1.4.zip`) — re-ran `sync-mods.ps1` to confirm it
reports "already up to date" against the real download now, not just adopting a local file.
Updated the `checked` note to drop the do-not-push warning.

Vanilla Armory is staying installed permanently (no longer just a test partner) — added a
`checked` entry recording that, and documented both mods in `docs/smithing-and-crafting.md` (new
Vanilla Armory section covering its bigger armor/weapon/relic roster, its maces getting the same
head/handle/binding treatment via Weaponsmith, and the Toolsmith `PartBlacklist` "armory" collision
Weaponsmith 1.1.0+ patches around). Regenerated README.md's Current Versions table (57 mods) and
added both mods to the Categories table.

Committed and pushed.
