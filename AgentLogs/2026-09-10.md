# 2026-09-10

## HUD overlap: HudClock vs xSkills Fork effect box

User noticed HudClock's top-left panel (date/temperature/wind/rift) overlapping the xSkills Fork "Gilded GUI" effect box (buff icons, e.g. "Well Rested") in the top-left corner.

- Measured HudClock's panel directly from user-provided screenshots (2560x1440): panel content runs from pixel (13,13) to roughly (400,109) — confirmed via pixel sampling at multiple columns/rows, both the top-left origin (13,13) and the bottom edge (108→109 sharp transition) reproduced consistently across three independent columns.
- Iteratively adjusted `ModConfig/XLeveling/gui/xSkillsGilded.json`'s `effectBoxOriginX`/`effectBoxOriginY` at the user's request: 8/8 (default) → 8/160 (too far down) → 8/130 → final 13/122, confirmed correct by the user in-game.
- Final result: effect box sits 13px below HudClock's measured bottom edge (109px) with the same 13px left margin HudClock uses — consistent padding between the two.
- Documented the overlap and fix in MOD_DB.json's `checked` entries for both `hudclockpatch` and `xskillsfork` per AGENTS.md's cross-mod-settings rule, so the recommendation isn't lost. This is a ModConfig/ (personal, gitignored) change only — no mod install/version change, nothing to push.
