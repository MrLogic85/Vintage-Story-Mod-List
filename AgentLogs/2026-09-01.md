# Session log — 2026-09-01

- No installs/updates/removals yet — initial audit only. Researched all 40 currently installed mods and populated MOD_DB.json with mod IDs, URLs, versions, fork/patch relationships, and known incompatibilities. No changes were made to the actual mod files.
- Updated `configlib` 1.10.14 -> 1.13.1 (user-approved; brings support up to game 1.22.7).
- Updated `vsimgui` (Dear ImGui) 1.1.16 -> 1.2.7 (user-approved; also required — configlib 1.13.1 needs vsimgui >=1.2.0).
- Decided (user call): keep both `commonlib` 2.8.0 and `commonlibforked` 2.8.1 installed permanently — mods depending on the original can't use the fork and vice versa, so this isn't a conflict to resolve.
- Restructured record-keeping: converted MOD_DB.md to MOD_DB.json (structured, current-state only — no dated history), and split LOG.md into this per-session logs/ directory with LOG.md as an index. Reason: MOD_DB.md had accumulated dated change history that belonged here instead.
- Renamed the per-session log directory from `logs/` (originally) to `ModLogs/` to `AgentLogs/` — the first name silently collided with Vintage Story's own `Logs/` folder on Windows' case-insensitive filesystem.
- Reworked MOD_DB.json schema (v1 -> v2), formalized in new MOD_DB.schema.json: added `installed` (bool), `forked_by` (inverse of `fork_of`), pulled `author`/`downloads` out as their own fields, and changed the old single `checked` object into an append-only `checked` array of point-in-time snapshots (version/date/downloads/known_incompatibilities/fork_decision/note) — meant for substantive rechecks, not routine version-bump noise, which still belongs here in the log instead. Null/empty fields are now omitted rather than written out.
- Added 7 new tracked-only (installed: false) entries for forks' originals that aren't installed but are worth periodically rechecking per AGENTS.md line 3: CarryCapacity, ProspectorInfo, SpecializedBags, Stone Quarry, Improved Handbook Recipes, PlayerCorpse, Xandu's Inventory Tweaks. All 7 confirmed unmaintained for game 1.22.x as of this check. Skipped adding entries for HUD Clock's and Durable Better Prospecting's predecessors — those are author-blessed renames/rewrites of the same lineage, not separate still-relevant originals.
- Installed two mods (user-requested) to fix the anvil-view freeze: `smithingplusbugfix` 2.0.1 (patch for SmithingPlus 1.9.0-rc.1) and `anvilperformanceoptimization` 1.0.0 (prewarms a vanilla engine cache). Both mods' release pages only list tags up to game 1.22.2/1.22.3, but their modinfo.json only requires game >=1.22.0 with no upper cap, and the SmithingPlus hotfix's own author recommends pairing them — installed both on game 1.22.7 per user decision despite the untested version gap.
- Created AgentBackupSaves/ (distinct from the user's own manual BackupSaves/ and Saves - Copy/) and added an AGENTS.md rule to back up Saves/ here at the start of each session before making mod changes, since a save is corruption-prone and mod changes are exactly the kind of thing worth a rollback point for. Backed up the save file (587MB) into AgentBackupSaves/2026-09-01/ before installing Temporals Reformed below.
- Installed `temporalsreformed` 0.2.0 (user-requested, enemy behavior overhaul). Researched Combat Overhaul as a possible pairing (user has it favorited but not installed): found the original (modid unlisted/delisted from search and from the author's own profile, page shows "Draft" status, last release only for game 1.21.x, history of crash reports across versions) is likely abandoned, while a community continuation "Combat Overhaul Fork" (combatoverhaulfork, assetid 48376) is actively maintained for 1.22.x. User decided to skip Combat Overhaul entirely (both the original and the fork) for now and stick with just Temporals Reformed — neither Combat Overhaul variant was added to MOD_DB.json.
- Researched Overhaul lib and Primitive Survival (user-requested, ~1M downloads each) — decided to skip both. Overhaul lib is a pure dependency library, near-useless standalone without Combat Overhaul or its addons (which the user isn't running). Primitive Survival's smoking/curing system (Smoker structure) turned out to duplicate a system Butchering (installed) already provides (its own Smoking Rack) — not a technical conflict, but redundant enough that the user chose to skip it despite otherwise strong thematic fit with Expanded Foods/A Culinary Artillery/Carry On/FoodShelves (all installed). Neither mod was added to MOD_DB.json.
- Confirmed vanilla Vintage Story already has a Raft and a Sailboat (https://wiki.vintagestory.at/Raft, https://wiki.vintagestory.at/Sailboat) — no mod needed for basic water travel.
- Installed `shipwright` 1.4.1 (adds 8 boat types spanning smaller-than-raft (Kayak, Coracle, Caballito de totora) to larger-than-sailboat (Drakkar, ~12 seats)). modinfo.json only requires game >=1.22.0 despite release tags only covering 1.22.0-1.22.3 — same pattern as the anvil-fix mods. Integrates with Config lib and Carry On (both installed).
- Set up the repo for git publishing at https://github.com/MrLogic85/Vintage-Story-Mod-List: added README.md (playstyle description + categorized mod table, cross-checked against all 44 installed entries in MOD_DB.json), and .gitignore excluding Mods/, all Saves*/Backup*/ directories, and personal config files — repo only ever tracks curation files (README, MOD_DB.json/.schema.json, AGENTS.md, LOG.md, AgentLogs/), never redistributes mod binaries or save data, since most mod authors don't grant redistribution rights outside mods.vintagestory.at.
- Wrote sync-mods.ps1: reads MOD_DB.json's installed mods and fetches each one directly from mods.vintagestory.at at the exact recorded version, tracking what it manages in a local .sync-state.json so reruns only touch mods it installed itself. Tested against an isolated scratch folder (not the real Mods/) covering fresh-install, no-op rerun, simultaneous update+removal, and bad-version-failure cases — all passed.
- Worked out and verified the correct git sequence for cloning into an already-populated existing folder (plain `git clone` refuses non-empty targets): `git init; git remote add origin <url>; git fetch; git checkout -t origin/main`. First draft (`checkout -- .` then manual `branch -M`/`-u`) was actually broken — tested it in a sandbox and it failed since `checkout -- .` never creates a commit. README now has the corrected, verified version with the real repo URL.
- Wrote play.ps1: git pull (--ff-only, warns and continues rather than blocking on failure/offline) + sync-mods.ps1 + launches Vintagestory.exe (auto-detected from common install locations, e.g. found this machine's install at %APPDATA%\Vintagestory\Vintagestory.exe). Syntax-checked only (parser, no execution) per user instruction not to run it on their behalf — it's meant for friends to self-serve via a one-time shortcut setup snippet added to the README, not something run on the host's machine as part of this session.
- Removed unverified Steam library paths from play.ps1's exe auto-detection candidates (user pointed out Vintage Story isn't distributed on Steam) — fabricated paths I hadn't actually confirmed, shouldn't have included them.
- Wrote setup.ps1 (-Path <exe path>, optional, auto-detects if omitted): creates a "Vintage Story (Modded)" desktop shortcut running play.ps1 with -GameExe baked in from -Path, replacing the inline PowerShell snippet previously in the README. Tested in an isolated sandbox (Desktop path redirected, fake exe) covering both a valid -Path (verified all four resulting shortcut properties: TargetPath, Arguments, WorkingDirectory, IconLocation) and an invalid -Path (clean failure, no fallback masking the bad input) — real desktop untouched.
- Rewrote the README's install section as a foolproof 4-step Win+R guide for non-technical friends (git init/remote/fetch/checkout -t + setup.ps1 -Path, chained into one paste block, with an explicit C:\Program Files\Vintagestory default the reader is told to edit rather than relying on auto-detection). Caught and fixed two real bugs in the user's draft by actually testing them: "-NoExit - Command" (stray dash gets parsed as `-Command -` = read from stdin, hard error, confirmed via direct execution) and `$env:AppData\Roaming\...` (double "Roaming" since $env:APPDATA already includes it, confirmed by printing the env var) plus a "VintageStordyData" typo. Ran the full corrected 4-step flow end-to-end against a real fake git remote and a pre-populated target folder (existing Mods/ file confirmed untouched, shortcut created correctly) before calling it done.
- Added a "Pushing a change" procedure to AGENTS.md (regenerate README's Current Versions section from MOD_DB.json -> stage only tracked curation files -> commit message must always include the full game version + complete installed-mods-with-versions list -> commit -> push -> log it). Added a matching "Current Versions" section to README.md (game version 1.22.7, all 44 installed mods + versions in a collapsible table).
- First real push to github.com/MrLogic85/Vintage-Story-Mod-List: discovered the user had already run the git init/remote/fetch/checkout -t setup on this machine themselves (repo was already tracking origin/main with one prior commit, "Create README.md with Vintage Story modlist"). Staged only the 9 expected curation files/dirs (verified via `git status` before staging that nothing from Mods/, Saves*/, Backup*/, or config files was present), committed as 67fb9d0 with the full game-version + 44-mod-with-versions message per the new AGENTS.md procedure, pushed to main, and confirmed via `git ls-remote` that the remote matches local.
- Privacy audit after user asked whether the push leaked anything: grepped all 10 pushed files for username/name/email/local-path patterns. Found two things: (1) both commits' git author/committer identity is the user's real name + personal email — this is just their own pre-existing global git config doing what git normally does, not something introduced by a file; user's call was to leave it as-is. (2) This log file itself (previous entry above) quoted the actual save file's name, which reads as a play on the user's surname — that one I introduced. User asked to fix it going forward only (not rewrite history): edited the line to say "the save file" instead of quoting its name; the original text is still visible in commit 67fb9d0's history if anyone checks it, which the user was informed of and accepted. Pushed as aaca896.
- Added a Git-availability check/auto-install to the front of the README's step-3 paste block (winget install --id Git.Git if git isn't found, with a PATH refresh so the same session picks it up, and a clear manual-install fallback message if winget also isn't available) — friends without Git previously hit an unexplained "'git' is not recognized" failure. Tested the detection logic both ways (git present -> correctly skips install and falls through to the already-verified init/remote/fetch/checkout sequence; git absent, simulated via a child process with git stripped from PATH -> correctly detects and would enter the install branch) without actually running a real winget install on this machine, since git is already present here and reinstalling it would be a pointless real system change.
- Wrote a docs/ folder with one page per README category (cooking-and-food, smithing-and-crafting, prospecting-and-mining, companions, exploration-and-world, danger-and-wildlife, quality-of-life, foundation-libraries) covering what each category's mods add, how they interact (cross-linked where relevant, e.g. Cartographer/Pet Map Markers, the CommonLib pair, Place on slabs/Terrain Slabs), and how/when to use them — grounded in MOD_DB.json's actual known_incompatibilities/fork_decision/note fields rather than written from memory. Linked all 8 from the README's Categories table. Verified every cross-link (docs-to-docs and README-to-docs) resolves to a real file before committing.
- User feedback: the docs/ pages were the wrong kind of content — MOD_DB.json's fields are curation notes (install/compatibility decisions), not gameplay info, and that's not what a "user guide" needs. Rewrote all 8 pages from scratch as actual how-to-play guides: named tools/items/structures, concrete crafting/interaction steps, keybinds, workflows (e.g. Butchering's hook->table process, Carry On's sneak+hold-right-click, Cartographer's /wp shareall). Researched via 5 parallel forks pulling each mod's real feature description from its mods.vintagestory.at page/API text field rather than reusing MOD_DB.json content. Confirmed the 5 Foundation-libraries mods (CommonLib, CommonLib-Forked, Config lib, Dear ImGui, Anvil Performance Optimization) have no player-facing gameplay at all and wrote that page honestly short rather than padding it out.
- Fixed two errors in the Cartographer section of exploration-and-world.md, caught by a user question: (1) `/waypoint shareall` is a one-time snapshot of currently-existing waypoints, not ongoing auto-share, confirmed via the mod's own v2.2.0 changelog ("works only on currently existing waypoints"); (2) `/wp share` is shorthand for the single-waypoint command (`/waypoint share <id>`), not shareall — the shareall shorthand is `/wp shareall`. Both had been stated wrong/ambiguously in the initial gameplay-guide rewrite; verified the correction directly against the mod page before fixing rather than assuming.
- User asked how /wp share interacts with Pet Map Markers' pet waypoints, which surfaced that the gameplay-guide rewrite had dropped a cross-mod compatibility note the original curation-style docs had. Checked Pet Map Markers' own page and comments for specifics on what "Weird Stuff" means — neither the author nor any commenter has documented the actual mechanism. Added an explicit warning to both companions.md and exploration-and-world.md not to share pet waypoints via Cartographer, with a clearly-labeled inference (not confirmed fact) about the likely cause: Pet Map Markers' waypoints are meant to keep moving with the pet, Cartographer's share commands hand out a frozen snapshot.
